# Jenkins CIC pipeline using docker as agent 

## Project Overview 
This project demonstrates how to use **Docker agent in jenkins pipeline** to run in an isolated an 
consistent environment .Each jenkins build run inside a docker container , ensuring depemdencies concistency and clean environment. 

## Technologies used 
- Jenkins 
- Docker
- Github 
- Python  
